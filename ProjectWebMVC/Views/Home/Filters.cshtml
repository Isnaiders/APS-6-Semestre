<<<<<<< HEAD
﻿@model FiltersViewModel
@{
	string imageName = Model.Image?.FileName;
=======
﻿@using ProjectWebMVC.Models.Enum
@model FiltersViewModel
@{
    string originImageName = Model.OriginImage?.FileName;
>>>>>>> origin/teste
}

<h1>@ViewData["Title"]</h1>

<h4>Program</h4>
<<<<<<< HEAD
<hr />
<div class="row">
	<div class="col-md-4">
		@*<form asp-action="Filters">
			<div asp-validation-summary="ModelOnly" class="text-danger"></div>
			<div class="form-group">
				<input type="submit" value="Create" class="btn btn-primary" />
			</div>
		</form>*@
	</div>
</div>

<div>
	<a asp-action="Index">Back to List</a>
</div>
<form method="post" enctype="multipart/form-data">
	<div class="row">
		<div class="col-md-4 col-lg-4">
			@*<button class="btn btn-default" type="button" value="Selecione a Imagem" onclick="@Url.Action("GetImage", "Home")"></button>*@
			@Html.TextBox("File", Model.File, new { @class = "form-control", type = "file", accept = "Image/jpg,Image/jpeg"})
		</div>
		<div class="col-md-4 col-lg-4">
			<button class="btn btn-default" type="submit" value="Aplicar"></button>
			<input class="btn btn-primary" type="submit" value="Aplicar" />
		</div>
		<div class="col-md-4 col-lg-4">
			<button class="btn btn-default" type="button" value="Baixar" onclick="@Url.Action("SaveImage", "Home")"></button>
		</div>
	</div>

	<label>Escolha a sua imagem para upload</label>
	<br />
	<br />
	<input type="file" name="Image" />
	<br />
	<br />
	<button type="submit" class="btn btn-danger">Enviar Imagem</button>
</form>

<form method="post" enctype="multipart/form-data">
    <label>Escolha a sua imagem para upload</label>
    <br />
	<br />
    <input type="file" name="Image" />
    <br />
	<br />
    <button type="submit" class="btn btn-danger">Enviar Imagem</button>
</form>

@if (imageName != null)
{
	<img src="/images/@imageName" />
}

@section Scripts {
	@{
		await Html.RenderPartialAsync("_ValidationScriptsPartial");
	}

	<script>
		$(document).on('change', '#textBoxImage', function () {
			console.log("entrei no change");
		})
	</script>
}
=======

@*@Html.ValidationSummary(false, "", new { @class = "text-danger" })*@

<form method="post" enctype="multipart/form-data">
    @Html.Hidden("OriginImage", Model.OriginImage)
    @Html.Hidden("FilteredImageName", Model.FilteredImageName)
    <div class="row">
        <div class="col-md-12">
            <div class="ibox">
                <div class="row">
                    <div class="col-md-4">
                        <div class="image-container">
                            @if (originImageName == null)
                            {
                                <img src="/img/no-image.jpg" id="image" />
                            }
                            else
                            {
                                <img src="/images/@originImageName" id="image" />
                            }
                        </div>
                        <div>
                            <input style="width: 100%; box-shadow: rgb(0 0 0 / 50%) 0px 2px 1px !important; margin-bottom: 10px;" type="file" name="OriginImage" accept="image/jpg, image/jpeg, image/png" />
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="form-group">
                            <label class="control-label"> Selecione o Filtro:</label>
                            <select asp-for="Type" class="drop-select" asp-items="Html.GetEnumSelectList<FilterType>()"></select>
                        </div>
                        <div class="col-md-12">
                            <div class="wrapper">
                                <button type="submit" class="btn-slide"><span>Aplicar</span></button>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="image-container">
                            @if (@Model.FilteredImageName == null)
                            {
                                <img src="/img/no-image.jpg" id="image" />
                            }
                            else
                            {
                                <img src="/images/@Model.FilteredImageName" id="image" />
                            }
                        </div>
                        <div>
                            @if (Model.FilteredImageName != null)
                            {
                                <button class="clean">
                                    <a href="/images/@Model.FilteredImageName" download><i class="fa fa-download"></i> Download</a>
                                </button>
                            }
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
>>>>>>> origin/teste
